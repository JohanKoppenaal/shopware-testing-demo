actor: AcceptanceTester
modules:
    enabled:
        - PhpBrowser:
            url: 'http://localhost:8000'
        - Asserts
# WebDriver configuratie (vereist ChromeDriver):
# modules:
#     enabled:
#         - WebDriver:
#             url: 'http://localhost:8000'
#             browser: chrome
#             window_size: 1920x1080
#             capabilities:
#                 chromeOptions:
#                     args: ["--headless", "--disable-gpu", "--no-sandbox"]
